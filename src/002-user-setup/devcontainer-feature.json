{
  "name": "User Setup",
  "id": "002-user-setup",
  "version": "0.0.2",
  "description": "Creates a non-root user with sudo privileges and forces zsh as default shell.",
  "installsAfter": [
    "ghcr.io/nikunh/001-dev-packages-vishkrm/001-dev-packages",
    "ghcr.io/devcontainers/features/common-utils"
  ],
  "options": {
    "username": {
      "type": "string",
      "default": "babaji",
      "description": "The username for the new user."
    },
    "password": {
      "type": "string",
      "default": "babaji",
      "description": "The password for the new user."
    }
  }
}
